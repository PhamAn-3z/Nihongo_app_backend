name: flashcard_quiz_backend
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  supabase: ^2.6.0
  dart_jsonwebtoken: ^2.12.2
  dotenv: ^4.2.0
  crypto: ^3.0.3
  intl: ^0.20.2
  http: ^1.2.2
  googleapis_auth: ^1.4.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
